// Deliberate lint violation (banned `any`) - verifies CI turns red. Never merge.
// eslint-disable-next-line no-unused-vars -- intentionally unused, the `any` is the point
export const brokenOnPurpose: any = "red";
