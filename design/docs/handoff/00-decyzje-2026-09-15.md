# Decyzje z 15.09.2026 — 18 sprzeczności blokujących z mapy luk (#195, PR #205)

Dawid, sesja 15.09.2026. Rozstrzyga wszystkie pozycje z `00-gap-map-blocking.md`. Tam, gdzie ten plik różni się od `claude-code-prompt-195.md`, `00-decyzje-2026-09-14.md` lub kitu, **ten plik wygrywa**. Ids jak w mapie. „Kit do poprawy” = zmiana w `design/` wchodzi razem z PR, którego dotyczy.

## A. Proces

| id | Decyzja | Skutek |
|---|---|---|
| Q-A01 | **13 PR wg mapy**: tokens → prymitywy → shell → dane/migracje → strona realizacji → profil → re-skiny. | Sekcja 7 mapy obowiązuje; prompt-00 §29 i prompt-195 §9 nieaktualne. |
| Q-A02 | **Osobny PR „shell”**: `ProfileBar`, `AccountMenu`, `VisitorMenu`, `LanguageChip`, `C-FOOTER`, skip link. Po tokenach i prymitywach, przed stroną realizacji. | Menu wg A6 (14.09), nie wg prompt-195 §3. |
| Q-A03 | **Strona realizacji przed profilem.** Nowa realizacja = strona realizacji w trybie edycji; `WorkCard` wchodzi w PR profil i linkuje do gotowej strony. Migracje `works.*` w PR dane/migracje. | `V-WORK-FORM` zastąpione trybem edycji `V-WORK`; testy formularza przenieść, nie kasować. |
| Q-A04 | **Wszystkie 5 kolumn zatwierdzone**: `works.slug`, `works.card_layout`, `works.r360_autorotate`, `works.description`, `profiles.avatar_shape`. **SPEC A12 poprawić w tym samym PR** (dane/migracje). | „Żadna reguła się nie zmienia” z prompt-195:19 nie obowiązuje dla A12. |

## C. Shell

| id | Decyzja | Skutek |
|---|---|---|
| Q-C02 | **Prawy slot strony realizacji jak na profilu**: `LanguageChip` (desktop, każdy widz), `AccountMenu` 40 px z pierścieniem 2 px `--surface-card`, hamburger dla wylogowanych na telefonie. Właściciel: ołówek + menu. | `WorkPage.jsx` `actions` do poprawy. |
| Q-C04 | **Opcje języka to linki** (jak dziś, piszą `NEXT_LOCALE`), w kontenerze `role="menu"` z `role="menuitem"` na linku, strzałki ↑↓, Escape, klik obok. Dotyczy chipa i pozycji języka w menu telefonu. | `Hero.jsx` `LanguageChip`: `listbox/option` → `menu/menuitem`; `e2e/i18n.spec.ts:38-39` bez zmian. |
| Q-C06 | **`C-FOOTER` wszędzie**: „Architectorium Sp. z o.o.” + „© 2026”, bez linków, bez języka (D-SHELL-9). | `PublicProfile.jsx:197`, `WorkPage.jsx:273`, `Footer.jsx` do poprawy; żadnych nowych stron. |
| Q-H02 | **Pasek strony głównej zostaje w `<main>`, w scrimie** (D-SHELL-12, axe). Prompt-195 §66 dostaje wyjątek dla strony głównej. | Skip link celuje w treść pod paskiem. |

## E. Panel „o nas”

| id | Decyzja | Skutek |
|---|---|---|
| Q-E01 | **Draft do ✓.** ✓ zapisuje nazwę, nagłówek, „O nas”, miejsca i kształt avatara jednym żądaniem; × odrzuca wszystko i zamyka panel. Miejsca (C7) też w drafcie — kolejność i dodanie/usunięcie idą razem z ✓. | D-PROFILE-8 zmienia się dla pól tożsamości; `PublicProfile.editHint` do przeredagowania; `profile-sections.spec.ts:105-112` do poprawy; leave guard pilnuje draftu. |

## F. Karta i kolejność

| id | Decyzja | Skutek |
|---|---|---|
| Q-F10 | **Grip tylko w trybie edycji właściciela** (B6), tylko przy >1 realizacji (D-WORKS-5). Okrągły 40 px obok ołówka na okładce (`[grip][ołówek]`, gap 8). Poza edycją: sam ołówek. | `WorkCard.jsx` do poprawy; `WORKS-LIST.owner.grip`, `.order.*`, `Works.card.move`, `works-order.spec.ts` zostają. |

## G. Strona realizacji

| id | Decyzja | Skutek |
|---|---|---|
| Q-G04 | **Do 10 zdjęć, 20 MB każde** (JPEG/PNG/WebP). Minimum 1. | SPEC A12 i A4-limit rozmiaru poprawić razem z Q-A04; `work_images` position check 0–9; `WORK_PHOTOS_MAX = 10`; `pl.json:508` do poprawy. Kopia kitu bez zmian. |
| Q-G05 | **300 znaków** — pole i kolumna. | `works.description` `≤ 300`; prompt-195:100 i :200 nieaktualne; kit bez zmian. |
| Q-G15 | **× odrzuca wgrane a niezapisane zdjęcia** (D-FORM-1). ✓ wysyła listę zdjęć i kolejność. Anulowana nowa realizacja nie zostawia plików. | prompt-195:161 nieaktualne; leave guard obejmuje wgrane zdjęcia. |
| Q-G17 | **Drugi kanał w pillu kafla edycji**: „Drugi kanał” (`layers` 16) dodaje; gdy jest — badge na kaflu i „Usuń drugi kanał”; progress jak w formularzu. | ID `WORK-FORM.photos.channel-add/-remove/-progress/-badge` i `Works.form.photos.channel*` zostają; test #99 przenieść do trybu edycji; `WorkPage.jsx:249-251` do poprawy. |

## H. Re-skin

| id | Decyzja | Skutek |
|---|---|---|
| Q-H01 | **Strona główna full-bleed** — zdjęcie fasady na cały pierwszy ekran za scrimem (jak `Hero.jsx` i dzisiejszy produkt). | prompt-195:181 („keep the split”) i `ui_kits/public-web/README.md:19` do poprawy. |
| Q-H08 | **Ekran dwuskładnikowy zachowuje produkt**: wybór metody (Aplikacja / Kod e-mail / Kod zapasowy), „Wyślij kod na e-mail” na żądanie. Re-skin tylko wyglądu. | `Auth.jsx:96-122` do poprawy; `two-factor.spec.ts` bez zmian; F-AUTH-18 (#59) pozostaje otwarte, nie wchodzi tu. |
| Q-H17 | **Ustawienia dwuskładnikowe zachowują produkt**: setup przez klucz + kody zapasowe + kod aktywacji, widok aktywny z kodami, hasło przy wyłączeniu. | `AccountSettings.jsx:35-130` do poprawy (wiersze metod → flow produktu). |

## J. Zrzuty

| id | Decyzja | Skutek |
|---|---|---|
| Q-J02 | **`matrix.json`: zrzut `…_edycja_avatar-kwadrat` startuje od czystego stanu** — własny `key` (albo reload), potem klik ołówka, potem klik przełącznika kształtu. Prompt-01 dopuszcza tę poprawę matrixa. | Zrzut pokazuje stan z `id`; 28 dalszych zrzutów odblokowane. |

## Do poprawy w `design/` (zbiorczo)

- `ui_kits/public-web/Hero.jsx` — `LanguageChip` na `menu/menuitem` z linkami (Q-C04).
- `ui_kits/public-web/PublicProfile.jsx`, `WorkPage.jsx`, `components/navigation/Footer.jsx` — stopka `C-FOOTER` (Q-C06); prawy slot na stronie realizacji (Q-C02).
- `components/profile/WorkCard.jsx` — grip w edycji (Q-F10).
- `ui_kits/public-web/WorkPage.jsx` — „Drugi kanał” w pillu kafla (Q-G17).
- `ui_kits/app/Auth.jsx`, `AccountSettings.jsx` — flow dwuskładnikowy z produktu (Q-H08, Q-H17).
- `ui_kits/public-web/README.md:19` — hero full-bleed (Q-H01).
- `docs/handoff/shots/matrix.json` — wpis `avatar-kwadrat` (Q-J02).
