# Decyzje z 14.09.2026 — nadpisania `claude-code-prompt-195.md`

Dawid, sesja 14.09.2026 (wieczór). Tam, gdzie ten plik i `docs/claude-code-prompt-195.md` się różnią, **ten plik wygrywa**. Numery sekcji odsyłają do prompt-195. Źródło prawdy dla każdego punktu: wskazany plik design systemu — wymiary czytaj stamtąd, nie z pamięci.

## A. Pasek — `ProfileBar` (§3.2)

| # | Było (prompt-195) | Jest | Źródło |
|---|---|---|---|
| A1 | Pasek `z-index: 50`, panel pod nim | Pasek + toolbar `z 44`, panel `z 45`, **logo (znak + nazwa + chevron) to osobna warstwa `fixed` `z 48`**. Panel wsuwa się *między* toolbar i logo, na każdej szerokości. | `components/profile/ProfileBar.jsx` |
| A2 | „No chevron” | **Chevron wrócił**: `chevron-right` 18 px po nazwie, obraca się 180° gdy panel otwarty (`--dur-3`). | ProfileBar.jsx |
| A3 | Grupa robi się czarna tylko na desktopie | Grupa czarna przy otwartym panelu **na każdej szerokości** (panel jest biały i leży pod nią). | ProfileBar.jsx `leftInk` |
| A4 | Właściciel: „+ Realizacja” w pasku | **Właściciel: okrągły ołówek 40 px** (biały 94 %, `--shadow-md`, `aria-label` „Edytuj profil”) → otwiera panel i tryb edycji; `disabled` + 50 % w trakcie edycji. Po nim `AccountMenu`. **„+ Realizacja” zniknęła z paska** (→ B6). | `ui_kits/public-web/PublicProfile.jsx` `actions` |
| A5 | Język: `LanguageChip` tylko dla wylogowanych/cudzych | **`LanguageChip` (flaga + nazwa + chevron, lista z flagami) na desktopie dla każdego widza**, także właściciela. Lista zamyka się klikiem obok i Escape, wyrównana do prawej. | `ui_kits/public-web/Hero.jsx` `LanguageChip`, `Flag` |
| A6 | Telefon: chip w panelu | **Telefon: wszystko pod jednym triggerem.** Zalogowani: avatar → menu „Profil, Konto, ─, Polski ✓ / English (z flagami), ─, Wyloguj”. Wylogowany: hamburger (`menu` 22 px) → „Polski / English, ─, Zaloguj się, Załóż konto”. Desktop menu avatara: „Profil, Konto, ─, Wyloguj” (język w chipie). Popover: 224 px, `--radius-lg`, `--shadow-lift`, pozycje 44 px, zamykanie: klik obok / Escape / wybór. **Język zniknął z panelu.** | PublicProfile.jsx `Popover`, `MenuItem`, `AccountMenu`, `VisitorMenu` |
| A7 | Strona realizacji: logo → powrót do profilu | **Strona realizacji: logo otwiera/zamyka panel „o nas”** (read-only, także w trybie edycji realizacji). Panel startuje pod paskiem (`start = bar`), avatar 120/96 px u góry (`top` 32/24). **Tylko duży avatar w panelu prowadzi do profilu** (`onAvatar`). | `ui_kits/public-web/WorkPage.jsx`, `AboutPanel.jsx` prop `onAvatar` |
| A8 | Strona realizacji, właściciel: „Edytuj” (`quiet`, tekst) | **Okrągły ołówek 40 px bez tekstu** (jak wszędzie). | WorkPage.jsx |
| A9 | Edycja realizacji: „Usuń realizację” / „Anuluj” / „Zapisz” (przyciski z tekstem) | **Same ikony, na każdej szerokości:** kosz (`trash-2` 18 danger, ghost), × (`x` 20 muted), ✓ (czarne kółko `--action-solid`, biała ikona; 40 % gdy nie da się zapisać). | WorkPage.jsx `actions` |

## B. Profil — strona (§4.1, §4.2)

| # | Było | Jest | Źródło |
|---|---|---|---|
| B1 | Pasek solid po zjechaniu tła | Bez zmian mechanicznie (IntersectionObserver), ale **avatar page'owy leży w warstwie `z 3` w paśmie tła**, pasek `z 44` — sprawdź, że avatar nie wjeżdża pod pasek (floor wysokości tła nadal obowiązuje). | PublicProfile.jsx |
| B2 | Avatar sam | **Obok dużego avatara nazwa** (`--type-h1`, telefon `--type-h2`, `--ls-heading`, `text-strong`), wyrównana do dolnej krawędzi avatara (padding-bottom 10/6). **Zanika przy otwieraniu panelu** (opacity 0 + `translateX(-12px)`, `--dur-3`), wraca przy zamykaniu; `aria-hidden` gdy ukryta. Wrapper `pointer-events: none`, sam avatar `auto`. | PublicProfile.jsx |
| B3 | Zdjęcia na karcie: badge ↗ na hover | **Zwykłe zdjęcia bez badge’a** — klik zawsze idzie do realizacji read-only. **↗ tylko na kaflu R360** (przycisk dolny-prawy). | `components/profile/WorkCard.jsx` `Photo` |
| B4 | Pill „Edytuj” z tekstem, prawy górny róg karty | **Okrągły ołówek 40 px bez tekstu**, zakotwiczony w bloku zdjęć (na okładce, prawy górny róg) w każdym układzie. | WorkCard.jsx `pencil` |
| B5 | — | **Avatar ma kształt:** `profile.avatarShape: "circle" \| "square"` (domyślnie koło). Kwadrat: promień skalowany z rozmiarem — `--radius-xs` < 48 px, `--radius-sm` < 96, `--radius-md` od 96. **Wszędzie ten sam kształt**: avatar na tle, kopia w panelu, avatar w menu konta (profil i strona realizacji). Pierścienie (`box-shadow 0 0 0 4px/2px`) dziedziczą promień. | `components/core/Avatar.jsx`, `.d.ts` |
| B6 | „+ Realizacja” w pasku | **„+ Realizacja” (`quiet`, `plus` 16, z tekstem) pod tłem, prawy górny róg kolumny realizacji** (`top = gap`, `right = avatarLeft`), **tylko właściciel w trybie edycji**. Empty state: „Wejdź w „Edytuj” i dodaj pierwszą plusem pod zdjęciem w tle. Nazwa i jedno zdjęcie wystarczą na start.” · "Open “Edit” and add the first one with the plus under the cover. A name and one photo are enough to start." | PublicProfile.jsx |

## C. Panel „o nas” — `AboutPanel` (§4.3, §4.4)

| # | Było | Jest | Źródło |
|---|---|---|---|
| C1 | Ołówek w prawym górnym rogu panelu | **Brak ołówka w panelu.** Edycję startuje ołówek w pasku (A4). | `components/profile/AboutPanel.jsx` |
| C2 | Telefon: panel nad paskiem (`z 56`, scrim `55`), × w rogu | **Telefon zachowuje się jak desktop**: panel `z 45`, scrim `z 44` (nad toolbarem, pod logo), **bez × w read-only** — zamyka logo lub avatar. Szerokość `min(100% − 88px, 320px)`. | AboutPanel.jsx |
| C3 | Telefon: te same fonty | **Telefon o stopień mniejszy**: h1→`--type-h2`, lead→`--type-body`, body→`--type-sm`, gap `--sp-6`, przyciski rogu 32 px (ikony 16). | AboutPanel.jsx `fH1/fLead/fBody/btn` |
| C4 | Plakietka w stopce (`margin-top: auto`) | **Plakietka to przyklejona stopka** (`position: sticky; bottom: -padding-bottom; flex-shrink: 0`, białe tło, hairline górny, ujemne marginesy boczne) — zawsze w widoku, w obu trybach. Uwaga: w kolumnie flex z overflow stopka bez `flex-shrink: 0` była ściskana do zera. | AboutPanel.jsx |
| C5 | Edycja = osobny formularz nad panelem (`IdentityEdit`, `z 46`, h2 „Edytuj profil”, pola `Input`/`Textarea`, „Zapisz/Anuluj” w stopce) | **Edycja = ten sam panel z ramkami.** Ten sam układ, ta sama typografia. Nazwa (h1), nagłówek (p) i „O nas” (p) są `contentEditable` w miejscu: **ramka wisi poza pudełkiem tekstu** — `padding: 2px 6px`, `border: 1px`, `margin: -3px -7px` — więc przepływ i zawijanie tekstu są identyczne z read-only do piksela; ramka `--border-default`, na focus `--focus-ring`, `--radius-control`, `white-space: pre-wrap`, `min-width: 3ch`, `min-height: 1lh`. Placeholder jako nakładka gdy pusto. Adres profilu i plakietka pozostają read-only; **plakietka śledzi pole nazwy na żywo**. Draft (`draft`, `onDraftChange`) trzyma rodzic; `IdentityEdit` usunięty. | AboutPanel.jsx `Framed`, props `editing/draft/onDraftChange/onSave/onCancel` |
| C6 | „Zapisz” / „Anuluj” w stopce | **✓ i × w prawym górnym rogu**: ✓ czarne kółko (`--action-solid`, `check` biały) zapisuje **i zostawia panel otwarty**; × anuluje **i zamyka panel**. Desktop: 40 px, wyśrodkowane w paśmie paska; telefon: 32 px, `top = bar + 8` (pod paskiem — inaczej zasłania je logo). | AboutPanel.jsx, PublicProfile.jsx `cancelEdit` |
| C7 | Miejsca: pole tekstowe + chipy z grip | **Chipy zostają; w edycji pinezka → `circle-x` 16 px (usuń)**. Na końcu rzędu **okrągły „+” 32 px** (`--surface-sunken`). Klik zamienia go w chip-pole (pinezka + input, `inset 0 0 0 1px --focus-ring`, `flex: 1 1 12ch`); od 2 znaków lista podpowiedzi **TERYT** pod rzędem (`left: 0; right: 0` rzędu chipów — nie wychodzi poza panel), max 10, nazwa + szczegół (miasto/gmina/powiat/województwo wg reguł D-PROFILE-10). Enter / Tab / klik dodaje **samą nazwę**; ↑↓ po liście; Escape lub puste pole po blur zwija do „+”. W kicie próbka ~25 miejsc — produkt podpina serwer. Grip/kolejność miejsc: bez zmian wobec spec. | AboutPanel.jsx `PlaceAdder`, `TERYT_SAMPLE` |
| C8 | Aparat 44 px dolny-prawy | **Lewy dolny łuk avatara, stałe miejsce niezależnie od kształtu**: `[przełącznik kształtu][aparat]`, oba 44 px, `left: 4; bottom: 4; gap 8`. Przełącznik pokazuje **kształt, który dostaniesz** (□ przy kole, ○ przy kwadracie), `aria-pressed`. | AboutPanel.jsx |
| C9 | Panel rozpoczyna edycję tylko z profilu | **Wejście w edycję zawsze otwiera panel** (`setAbout(true)`); po edycji panel zostaje otwarty (✓) lub zamyka się (×). | PublicProfile.jsx `startEdit` |

## D. Dane przykładowe

Kit pokazuje **XOSA Architekci** (logo jako avatar, tło: czarno-biała marina, 4 realizacje z prawdziwymi renderami: HNB Białobrzegi z R360, DLO Lisi Ogon, MVG Gniezno, DPB Bydgoszcz). Pliki w `assets/xosa/`, dane w `ui_kits/public-web/data.js`. To dane demo — do seedów i zrzutów, nie do produkcji.

## E. Nietknięte

Tokeny (§1, §2), plakietka #199 (§1.3), tło zawsze obecne (§4.1 pkt 2), trzy układy karty (§4.2), strona realizacji poza paskiem (§5.1, §5.2, §5.3 pkt 1–4), re-skin reszty (§6), findings (§7), pytania otwarte (§8).
