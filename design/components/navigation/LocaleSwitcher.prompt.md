Two-language switch, always visible on public pages.

```jsx
<LocaleSwitcher current="pl" onChange={setLocale} />
```

- Polish first — it is the product's home market and default locale.
- No flags, no globe icon, no dropdown.
