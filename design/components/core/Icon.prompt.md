Tinted Lucide glyph — the only icon source in this system.

```jsx
<Icon name="map-pin" size={20} tone="muted" />
```

- Icons are decorative (`aria-hidden`); the label next to them carries the meaning.
- Never mix another icon family in, and never hand-draw an SVG.
