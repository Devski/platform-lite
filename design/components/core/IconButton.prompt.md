Square 40px control for a single icon action — edit, copy, close, menu.

```jsx
<IconButton icon="pencil" label="Edit display name" />
```

- `label` is mandatory; the icon alone never communicates the action.
- `tone="onPhoto"` for the hero; `tone="bordered"` when it sits alone with no adjacent button.
